#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{
    // queue<int> q1;//create an empty queue q1
    // queue<int> q2(q1);//create Q q2 by copying elements of q1
    //-------------------------
    // q.empty() ==> return true if q is empty
    // q.push(value) ==> push the value into the queue.
    // q.pop() ==> remove the front element of the queue
    // q.front() ==> return the front element of the queue but not remove.

    return 0;
}

